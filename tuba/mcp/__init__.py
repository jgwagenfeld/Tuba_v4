"""Tuba v4 Model Context Protocol (MCP) package: run ``python -m tuba.mcp.server``.

Nothing is imported here: importing the server first would make ``python -m`` run a
second copy of it (runpy warns that the module is already in sys.modules).
"""
