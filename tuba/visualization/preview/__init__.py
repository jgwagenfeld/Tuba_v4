"""The studio's local HTTP/WebSocket transport.

:mod:`tuba.visualization.preview.transport` holds the transport (broker, routes,
watcher); :mod:`tuba.visualization.preview.server` holds ``ProjectStudioServer``,
which serves a project folder's Build and Review bundles on it.
"""
